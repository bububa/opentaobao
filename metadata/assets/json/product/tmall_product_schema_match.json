{
	"requestParams": [
		{
			"name": "category_id",
			"type": "Number",
			"description": "商品发布的目标类目，必须是叶子类目",
			"required": true
		},
		{
			"name": "propvalues",
			"type": "String",
			"description": "根据tmall.product.match.schema.get获取到的模板，ISV将需要的字段填充好相应的值结果XML。",
			"required": true
		}
	],
	"responseParams": [
		{
			"name": "match_result",
			"type": "String",
			"description": "返回匹配产品ID，部分类目可能返回多个产品ID，以逗号分隔。"
		}
	],
	"name": "tmall.product.schema.match",
	"apiChineseName": "product匹配接口",
	"description": "根据tmall.product.match.schema.get获取到的规则，填充相应地的字段值以及类目，匹配符合条件的产品，返回匹配product结果，注意，有可能返回多个产品ID，以逗号分隔（尤其是图书类目）；",
	"id": 23259
}