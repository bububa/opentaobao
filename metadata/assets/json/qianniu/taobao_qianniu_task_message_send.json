{
	"requestParams": [
		{
			"name": "task_id",
			"type": "Number",
			"description": "任务ID。如果taskid不为空，则只发给task对应的单个接收人。如果taskid为空，则发给metadata_id对应的所有接收人。"
		},
		{
			"name": "metadata_id",
			"type": "Number",
			"description": "任务元id，如果taskid不为空，则只发给task对应的单个接收人。如果taskid为空，则发给metadata_id对应的所有接收人。"
		}
	],
	"responseParams": [
		{
			"name": "result",
			"type": "Boolean",
			"description": "是否成功"
		}
	],
	"name": "taobao.qianniu.task.message.send",
	"apiChineseName": "发送任务提醒消息",
	"description": "如果taskid不为空，则只发给task对应的单个接收人。如果taskid为空，则发给metadata_id对应的所有接收人。消息会以任务消息的形式发给客户端。",
	"id": 22593
}